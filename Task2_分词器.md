## 2.1训练分词器

分词器常被视为LLM的一部分，但拥有独立的训练生命周期。需要运用正则表达式对原始文本进行预处理，并统计构建出一套高效的词元——数字离散序列转化

训练一个用于现代大型语言模型的分词器可以拆成四步：

1. 准备语料
2. 初始化基础单元（可忽略）
3. 统计并迭代合并
4. 输出产物并用于编码、解码

#### 2.1.1 准备语料

1. 在准备语料阶段，应尽量收集覆盖目标应用场景的多样化文本，以便训练出的词表对下游任务具有良好泛化能力
   - 准备不同类型的文本信息比如小说、散文、诗歌等不同描述风格的信息
   - 多种语言的文本信息比如中文、英文、韩语、法语等
2. 在原始文本进行清洗和标准化是必须的步骤，包括去除或屏蔽无关元数据、修正或删除乱码与非法字符、统一字符编码为UTF-8，并对重复或近重复样本进行去重以减少训练偏移
3. 对带有敏感信息或隐私的语料要提前进行脱敏处理与合规检查
   - **NER：**从大量非结构化的文本中，自动识别并提取出具有特定意义的实体
   - **保护隐私**和**保留关键语义信息**之间进行合理的策略选择与权衡
4. 在多语言或混合语料场景中，应统计各语言占比，并评估是否对低资源语言进行过采样或定向保留，避免词表被高频语言主导

#### 2.1.2初始化基础单元

- 预分词：将原始文本切分成可统计、可合并的基础单元，例如字符、字节或Unicode片段

  - 基于空格和标点的切分策略

  - Unicode类别划分策略：不同类别会进入不同的token块

  - 字节级切分策略：先将每个字符拆成UTF-8字节序列，不依赖语言种类、字符，按照单个字节序列得到一个独立的token

**补：Unicode与UTF-8的联系**：Unicode就像给全世界所有字符发的“身份证号”，但”身份证号“本身只是一个抽象编号，电脑不能直接存储；UTF-8将这个字符对应的”身份证号“写进电脑的具体方式，两者配合起来让自然语言可以被计算机准确存储、传输和解析。

在LLM的token划分中，常见策略包括：

1. 基于规则的预分词（如按空格和标点切分）
2. 按Unicode类别分段（如连续汉字、连续拉丁字母或数字）
3. 更底层的UTF-8字节级切分

相比之下，UTF-8字节级策略具有最强的通用性，它把任意文本统一拆分字节序列，从而从根本上减少未词汇表外（OOV）问题并覆盖任意字符集。但因为它以最细粒度开始，训练时通常需要更多轮的共现统计与合并来把零散字节压缩成紧凑且语义的token，才能在Transfomer计算效率与语义表征之间的取得平衡。

- 对大多数以空格为词边界的语言，可先用正则表达式按单词边界和标点进行初步切割，而对中文、日文等不以空格为词界的语言则通常采用逐字符或基于字的初始单元来保证覆盖率

  字节级预分词的好处：

  - token利用率更高
  - 可兼容处理多种语言
  - 学到更多高频片段，减少未登录词的出现情况，模型推理更快（token数少）

- 预分词生成的基础单元序列将作为后续统计合并到输入，务必保存该序列与对应位置以便在训练过程反复高效更新。



#### 2.1.3统计并迭代更新

1.子词候选统计

遍历语料以收集用于后续决策的统计信息，具体方法随算法不同而异

- BPE：统计当前字符、子词序列中相邻对的出现频次，每次贪心合并出现频次最高的相邻对，迭代构建词表——其决策仅基于频率统计。
- WordPiece：评估合并或保留某些子词对对语料似然即语言模型性能的贡献，选择能显著提升语料拟合度的合并操作。
- Unigram：从一个过大的种子词表出发，初始化每个token的概率。
- SentencePiece：是一个语言无关的子词分词框架，提供统一的训练与编码流程，支持多种分词算法（如BPE和Unigram）。这些算法在同一框架下独立使用，而非直接融合使用，其互补性体现在不同任务和数据条件下的适用性差异。

| 算法                                                         | 适用场景                                       | 常见实现方式                                                | 典型LLM / 模型                                               |
| ------------------------------------------------------------ | ---------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------ |
| [BPE](https://arxiv.org/pdf/1508.07909)                      | 简单高效； 高频子词压缩效果好； 适合大规模语料 | 字节级BPE 代码点级BPE                                       | GPT-2、GPT-3、GPT-4（tiktoken体系） LLaMA（改进BPE） RoBERTa |
| [WordPiece](https://huggingface.co/learn/llm-course/en/chapter6/6) | 控制词表大小； 减少OOV； 适合MLM类模型         | 字符级、代码点级                                            | BERT（原始实现） DistilBERT 早期RoBERTa（兼容）              |
| [Unigram LM](https://arxiv.org/abs/1808.06226)               | 概率建模子词； 对低频词更友好； 多语言适配强   | SentencePiece（Unigram模式） 支持byte-fallback              | T5、mT5 UL2 Gemma（Google系）                                |
| [SentencePiece](https://arxiv.org/pdf/1804.10959)（框架）    | 语言无关； 端到端训练tokenizer； 适合多语种    | **BPE或 Unigram的训练框架**（不是新算法） 支持byte-fallback | LLaMA（使用SP-BPE） DeepSeek系列                             |

**总结：**分词器训练的核心是【迭代更新候选子词—控制词表大小或收敛指标—监控质量指标，不同算法仅在“候选生成方式”和“迭代更新策略”上有差异

#### 2.1.4 输出产物并用于编码和解码

1. **导出核心产物：**训练完成后需要导出至少至少两个关键文件：
   - vocab文件：记录所有token及其对应的id，是编码器和解码器的核心索引
   - merges文件：按顺序记录所有合并规则及或概率模型。二者共同决定tokenizer的编码与解码逻辑，并确保编码的可逆
2. 下游使用前的验证和评估
   - 将tokenizer应用于一部分验证集后，建议统计以下关键指标：平均token数与最大长度分布、碎片化情况、跨语言token平衡度
   - 后续扩表：增量训练、加入新的merges项、清理极低频token
   - 扩表后回归测试：与旧模型兼容并根据数字化编码可还原，不发生token分配冲突或耗尽问题
3. 版本管理和可复现性保证：词表和merges文件应纳入严格的版本控制



### 2.2 常用的分词器

#### 2.2.1字符分词器

原理介绍：直观、简单，将文本拆解为最小的字符单位

- 优点：词表极小
- 无OOV问题
- 缺点：
  - 序列过长：一句话变成字符后，长度增加数倍，消耗LLM宝贵的上下文窗口
  - 语义稀疏：由于单个字符不具备独立语义，模型需要更深的网络层数来组合出意义

#### 2.2.2字节分词器

原理介绍：计算机底层存储文本本质上都是**字节**，在UTF-8编码中，英文通常占一个字节，汉字通常占三个字节。字节分词器直接对二进制字节进行操作

- 核心逻辑：不再维护“字符”的词表，而是维护一个大小为256的基础词表（0x00到0xFF）
- 应用： 现代LLM通常不单独使用纯字节分词，而是将字节作为BPE的基础单位即BBPE（可以彻底解决跨语言和特殊符号的编码问题）

**字节集分词器的压缩比恒等于1**

- 输入文本中单个字符首先被编码为UTF-8字节序列
- 字节级分词器将每一个UTF-8字节（0-255）直接作为一个token
- 因此token数量=UTF-8字节数

**压缩比=原始字节数/token数量**



#### 2.2.3词级分词器

原理介绍：基于空格（英文）或分词算法（中文）将文本切分为具备独立语义的“词”

- 优点：Token保留了完整的语义信息
- 缺点：
  - 词表爆炸
  - OOV问题严重

#### 2.2.4BPE分词器

原理介绍：目前LLM最主流的分词算法，BPE是一种试图在字符级和词级之间找到平衡

- 核心思想：统计预料中相邻字符对出现的频率，迭代地将最频繁出现的字符对合并成新的token

- 过程：

  1. 初始化：将单词拆成字符序列
  2. 统计：计算相邻字符对的频率
  3. 合并：将频率最高的对合并成新Token
  4. 循环：重复上述步骤，直到达到预设的词表大小

  

## 2.3 分析DeepSeek的分词器                                                                                                                                                                      

**总结：**分词器中的`token ↔ id`映射只描述“这个token的内容”，并不包含它在句子中的任何位置信息。BPE或其他基于统计和概率的分词算法，其本质都是依据语料中的共现频率或概率分布，决定如何将常见的字符、字节或子串合并成更长、更高频的token。这类算法本身并不理解句子的语义，它更像一个纯统计模块，通过频率或概率原则对字符序列进行切分与合并，为模型*LLM*提供稳定而紧凑的离散输入单元。